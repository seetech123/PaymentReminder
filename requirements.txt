# All free — no paid API subscriptions needed
groq>=0.9.0           # Free LLM API (Llama 3.3 70B)
streamlit>=1.35.0     # Web UI — opens in browser, no HTML/CSS needed
pandas>=2.0.0         # Excel + data processing
openpyxl>=3.1.0       # .xlsx read/write support for pandas
python-dotenv>=1.0.0  # Load .env file
# smtplib, csv, json, tempfile are built into Python — no install needed
